c++ or python??

for generating strings:
    maintain a hash table tokens {token, cheese string}
    getCheese(string token)
        if token isn't in tokens, generate a new string and add it to tokens.
        return tokens[token].

separate file into tokens
    types of tokens:
        word: string of alphabet, numbers, underscores
        spaces
        newlines
        operators
     
    special:
        quotes
        backslashes
        #
        comments

in new file:
    add the #defines
    write each translated token
